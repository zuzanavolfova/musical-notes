/**
 * Common Types
 * Shared types, unions, and enums used across the application
 */

export type TabType = "Notes" | "Keyboard";

export type DialogSize = "S" | "M" | "L";

export type ButtonSize = "S" | "M" | "L";


